# Session `20260803-2311-textbook-docs`

## 元数据

- 任务：`TASK-DOC-002`
- 开始时间：2026-08-03T23:11:30+08:00
- 结束时间：2026-08-03T23:20:07+08:00
- Base revision：`5780abb`
- Claim：`docs/claims/closed/20260803-2311-textbook-docs.md`
- Continues：`20260803-1911-m2-long-mode`

## 接手状态

- M0、M1、M2 已实现，已有 build/boot 参考文档和 ADR，但缺少按里程碑组织、
  面向学习者的完整教科书章节。
- 当前工作区保留尚未提交的 M2 实现与交接文件，本会话不修改 M2 代码。
- 无 active claim，项目状态检查通过。

## 目标

- 定义统一的里程碑教材结构与完成标准。
- 为 M0、M1、M2 补写背景、实现方法、示意图、实验和拓展阅读。
- 将教材章节纳入 M3-M10 的里程碑验收规则。

## 实际修改

- 新增 `docs/textbook/README.md`，建立 M0-M10 阅读路线、图示约定和拓展阅读
  来源规范；新增可复用的章节模板与完成检查表。
- 补写 M0 教材：从 hosted/freestanding 背景讲解编译、链接、ELF、VMA/LMA、
  镜像制作、构建安全和可重复验证。
- 补写 M1 教材：从 BIOS 启动契约解释 `CS:IP`、`DS/ES/SS:SP`、方向标志、
  boot drive、DAP/LBA、重试、`S2OK` 和错误注入。
- 补写 M2 教材：按 real/protected/long mode 顺序讲解 A20、E820、ELF
  `PT_LOAD`、GDT、四级页表、higher-half、`boot_info`，并区分 cache/TLB miss、
  page fault 和页面置换。
- 每章包含流程图、精确地址图、验证、误解与排错、限制、练习，以及经过核对的
  Intel、GNU、Linux、QEMU、UEFI 官方拓展阅读入口。
- 将教材 `verified` 纳入 `plan.md` 的所有里程碑共同验收规则，并在 README 增加
  教材入口。
- 图文核对发现 M2 为 32 张 higher-half PT 只填写 8192 个 PTE；新增常量并改为
  填写 16384 个 PTE，使代码与 ADR 的 64 MiB 映射一致。

## 验证

| 命令/检查 | 结果 | 关键输出或说明 |
|---|---|---|
| 教材本地链接检查 | `PASS` | README、plan、M0-M2 章节的仓库内链接均存在 |
| 教材章节结构检查 | `PASS` | 三章均有 Mermaid、验证、常见误解、练习和拓展阅读，状态为 `verified` |
| Markdown fence 检查 | `PASS` | 所有代码/Mermaid fence 成对 |
| `nm -a build/boot/stage2.o` | `PASS` | `HIGH_PTE_COUNT = 0x4000`，即 16384 PTE/64 MiB |
| `make image && make verify` | `PASS` | stage2 1925 B；ELF entry 与镜像布局通过 |
| `make test-boot` | `PASS` | 正常、损坏 stage2、非法 ELF、低内存四条路径通过 |
| `git diff --check` | `PASS` | 无 whitespace error |
| `./tools/check-project-state.sh` | `PASS` | 关闭前 1 个 active claim，无 scope 冲突 |

## 未完成与风险

- Mermaid 依赖 Markdown 渲染器；每张图同时有文字说明和等宽布局，纯文本阅读不会
  丢失唯一信息。
- 外部资料会持续更新，后续修改相应里程碑时需要重新核对版本和链接。
- M3-M10 尚未实现，因此不预建空章节；实现时从模板创建并随代码验收。
- 当前工作区还包含未提交的 M2 与本会话文档/页表修正，本会话未提交或推送。

## 交接建议

- M3 实现会话应先从 `docs/textbook/TEMPLATE.md` 创建对应章节，并让图文、测试与
  console/IDT/PIC/PIT/键盘实现同步演进。
