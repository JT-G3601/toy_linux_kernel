# Session Logs

每个写会话使用一个独立 Markdown 文件，文件名为 `<session-id>.md`。日志回答五个问题：

1. 会话接手时项目处于什么状态？
2. 本次实际修改了什么？
3. 执行了哪些验证，结果是什么？
4. 哪些内容没有完成或失败了？
5. 下一会话最安全、最明确的动作是什么？

日志不复制大段命令输出，只保留命令、关键结果和必要错误。聊天中说过但没有写进项目日志的信息，不视为可靠交接信息。

