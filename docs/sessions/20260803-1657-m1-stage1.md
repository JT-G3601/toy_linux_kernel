# Session `20260803-1657-m1-stage1`

## 元数据

- 任务：`TASK-M1-001`
- 开始时间：2026-08-03T16:57:53+08:00
- 结束时间：2026-08-03T17:10:38+08:00
- Base revision：`e1da9c5`
- Claim：`docs/claims/closed/20260803-1657-m1-stage1.md`
- Continues：none

## 接手状态

- `TASK-M0-001` 已为 `done`，`TASK-M1-001` 为 `todo`。
- 当前分支为 `agent/m0-build`，HEAD `e1da9c5`，跟踪同名远程分支。
- 无 active claim；工作区有上一已关闭会话留下的 linker 中文注释及交接文件，
  本会话保留且不改写这些内容。
- ADR-0003 保留 LBA 0 给 stage1、LBA 1..127 给 stage2，kernel ELF 固定在 LBA 128。

## 目标

- 实现 512-byte stage1，初始化寄存器/栈并保存 BIOS boot drive。
- 使用 INT 13h extensions 读取 stage2，具备重试、错误码和 stage2 头校验。
- 提供可观察的 `S1`/`S2` VGA 与 COM1 输出。
- 在构建期检查 boot sector 尺寸、`0xAA55` 签名和镜像布局。
- 验证正常交接和损坏 stage2 时的拒绝跳转路径。

## 实际修改

- 新增 `boot/stage1.S`：规范化 `CS`，初始化 `DS/ES/SS:SP`，保存 BIOS boot
  drive，初始化 COM1，并同时向 VGA/COM1 输出状态码。
- stage1 检查 INT 13h extensions，用 `AH=42h` 将 LBA 1..127 读到 `0800:0000`，
  失败后执行 disk reset 并最多尝试三次；每次重试前重置 DAP sector count。
- stage1 用 `S2OK` 头阻止明显无效的 stage2，分别以 `E1`/`E2` 表示磁盘/
  交接头错误，成功后保持 `DL` 并跳至 `0800:0004`。
- 新增一个 512-byte M1 stage2 占位程序，输出 `S2` 后停机，为 M2 保留最多
  127 个扇区的增长空间。
- Makefile 新增 `boot` 和 `test-boot` 目标，构建期强制 stage1 为 512 字节且包含
  `0xAA55`，并把 stage1/stage2 纳入镜像依赖。
- 扩展 `mkimage.sh` 与 `verify-image.sh`，检查 boot 产物大小、签名、交接头、
  保留区和镜像中的逐字节一致性。
- 新增 `tools/test-boot.sh`，自动运行正常镜像与临时损坏 `S2OK` 的镜像；
  同时在 doctor 中检查新使用的 `od`/`timeout`。
- 新增 ADR-0004 和 `docs/boot.md`，记录磁盘读取、实模式加载地址、交接头、
  错误码和 M2 继承契约；同步更新 README 与构建文档。
- 将 `TASK-M1-001` 标记为 `done`，并把已验证的 M1 事实写入项目状态。
- 保留了上一会话中尚未提交的 `kernel/linker.ld` 中文注释和其交接文件。

## 验证

| 命令/检查 | 结果 | 关键输出或说明 |
|---|---|---|
| `./tools/project-context.sh` | `PASS` | 完成启动上下文检查，无 active claim |
| `make doctor` | `PASS` | 0 errors；GDB 缺失为 1 个 optional warning |
| GCC `make verify` | `PASS` | stage1 512 bytes/`0xAA55`，stage2 512 bytes/`S2OK`，ELF 和 16 MiB 镜像布局通过 |
| GCC `make test-boot` | `PASS` | 正常 `S1 -> S2`；损坏头 `S1 -> E2`，且未进入 stage2 |
| Clang `make verify` | `PASS` | boot/kernel/image 静态校验通过 |
| Clang `make test-boot` | `PASS` | 正常和损坏 stage2 路径均通过 |
| GCC/Clang boot binary `cmp` | `PASS` | stage1 与 stage2 在两个工具链下分别逐字节相同 |
| `make clean && make verify && make test-boot` | `PASS` | 从干净默认构建目录完成 GCC 构建与 QEMU 测试 |
| GCC 独立构建目录与 `cmp` | `PASS` | stage1/stage2/kernel/image 全部逐字节可复现 |
| `bash -n tools/*.sh` | `PASS` | shell 脚本语法通过 |
| `git diff --check` | `PASS` | 无 whitespace error |

## 未完成与风险

- stage2 仍是占位程序，尚不会读取 kernel ELF、建立页表或进入 long mode。
- `S2OK` 只校验固定交接头，不是整个 stage2 的 checksum。
- `E1` 重试与停机路径已实现，但本会话没有为 QEMU 注入底层 BIOS 读盘失败，
  因此该路径的动态故障注入测试为 `NOT_RUN`。
- GDB 客户端仍未安装，不影响 M1 验收。

## 交接建议

- 下一步执行 `TASK-M2-001`，在保留 `S2OK` 头、`0800:0004` 入口和 `DL` boot
  drive 契约的前提下扩展 `boot/stage2.S`。
- M2 应从 LBA 128 读取并解析 ELF program headers，不应把整个 ELF 当作平坦
  binary 直接执行。
